@model IEnumerable<string>

@if (Model != null && Model.Any())
{
    <div class="tags">
        @foreach (var tag in Model)
        {
            <a class="label label-primary"
               onclick="event.stopPropagation()"
               asp-controller="Search" 
               asp-action="" 
               asp-route-query="@tag" 
               asp-route-isTag="true">
                #@tag
            </a>
        }
    </div>
}
