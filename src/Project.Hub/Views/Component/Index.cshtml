@using Project.Hub.Config.Entities.Common
@using Project.Hub.Config.Entities.Common.Component

@model ComponentDetails

@{
    ViewData["Title"] = Model.Name;
}

<h2>@Model.Name</h2>
<p class="lead">@Model.Description</p>
@Html.Partial("_Tags", Model.Tags)

@Html.Partial("_SectionHeader", new BaseConfig("Environments"))
@Html.Partial("_Environments", Model.Environments)
