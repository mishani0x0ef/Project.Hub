@model Project.Hub.Config.Entities.BaseConfig

<div class="row">
    <div class="col-xs-12">
        <h3>@Model.Name</h3>
        <div class="header-underline underline-lg"></div>
        @if (!string.IsNullOrEmpty(Model.Description))
        {
            <p class="lead">@Model.Description</p>
        }
    </div>
</div>
